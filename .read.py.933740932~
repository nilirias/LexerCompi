from os import DirEntry, read, getcwd
from pathlib import Path
import io
import sys
from direcciones import *

dir_memoria = [
  #NUM  WORD   BOOL
  [  0,  500,  1000 ], #GLB
  [ 1500, 2000, 2500], #LCL 1500
  [ 3000, 3500, 4000], #TEMP
  [ 4500, 5000, 5500]  #CTE
]
pointersMemoria = [[for _ in range]] #[0] = NumGLB, [1] = WordGLB, [2] = BoolGLB


if len(mapa_memoria) == 0:
  mapa_memoria = [None] * 6000

file_obj = open("MAIN.miku", "r")
for line in file_obj:
  curSentence = line.split()
  print(curSentence)
file_obj.close()